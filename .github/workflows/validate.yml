name: Validate Raahi Mini

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      NEXT_PUBLIC_SUPABASE_URL: https://example.supabase.co
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ci-placeholder-key
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package.json
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Type check
        run: npm run type-check
      - name: Validate Raahi-Test scripts
        run: |
          node --check Raahi-Test/src/config.mjs
          node --check Raahi-Test/src/setup-auth.mjs
          node --check Raahi-Test/src/check-auth.mjs
          node --check Raahi-Test/src/smoke.mjs
      - name: Production build
        run: npm run build
