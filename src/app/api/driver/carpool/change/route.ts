import { NextResponse } from "next/server";
import { z } from "zod";
import { createClient } from "@/lib/supabase/server";
import { mapCarpoolError } from "@/lib/carpool-api";
const schema=z.object({journeyId:z.string().uuid(),destinationLocationId:z.string().uuid(),departureAt:z.string().datetime(),idempotencyKey:z.string().min(8).max(200)});
export async function POST(request:Request){const correlationId=crypto.randomUUID();const parsed=schema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return NextResponse.json({ok:false,code:"VALIDATION_FAILED",message:"Material change is invalid.",correlationId},{status:400});const supabase=await createClient();const{data:claims}=await supabase.auth.getClaims();if(!claims?.claims?.sub)return NextResponse.json({ok:false,code:"UNAUTHENTICATED",message:"Sign in to propose a Carpool change.",correlationId},{status:401});const i=parsed.data;const{data,error}=await supabase.rpc("propose_material_carpool_change",{p_journey_id:i.journeyId,p_destination_location_id:i.destinationLocationId,p_departure_at:i.departureAt,p_idempotency_key:i.idempotencyKey});if(error){const hit=mapCarpoolError(error.message);if(hit)return NextResponse.json({ok:false,code:hit[0],message:hit[2],correlationId},{status:hit[1]});console.error("propose_material_carpool_change failed",{correlationId,code:error.code});return NextResponse.json({ok:false,code:"COMMAND_FAILED",message:"Raahi could not propose this change.",correlationId},{status:500});}return NextResponse.json({ok:true,value:data,correlationId});}
